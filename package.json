{
  "name": "nigredo-astro",
  "version": "1.3.3",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npx esbuild public/main.js --minify --target=es2017 --outfile=public/main.min.js && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
